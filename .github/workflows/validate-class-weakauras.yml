name: Retired - Class WeakAura Validator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Class-native HUD validation now lives in publish-beta.yml."
